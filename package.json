{
  "name": "crdt-sync-engine",
  "version": "1.0.0",
  "type": "module",
  "description": "Hand-rolled JSON-CRDT (maps + lists) with a WebSocket sync layer: offline editing, reconnect merge, and a formal shuffled-order convergence proof.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "start": "node server.js",
    "demo:reconnect": "node demo/reconnect-scenario.mjs",
    "demo:stress": "node demo/reconnect-stress.mjs"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}